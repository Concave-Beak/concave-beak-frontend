services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    container_name: app_dev
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "5173:5173"
